import { Alert } from 'reactstrap';

export function Versions() {
  return (
    <Alert color="info" className="mb-0">
      The Versions view is being ported in the next change. Until then, switching versions is
      unavailable from the UI.
    </Alert>
  );
}
